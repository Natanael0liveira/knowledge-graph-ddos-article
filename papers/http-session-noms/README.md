# NOMS submission

*Session-Centric Knowledge Graphs for Explainable Detection and Scoped Mitigation
of Distributed Application-Layer DDoS.*

## Layout

```
pp. 1–8    main text (Sections I–VII)   <- AT the 8-page limit
pp. 9–10   references (23 entries)
pp. 9–11   appendices A–E
total      11 pages                     <- limit is 12
```

Appendices: **A** runtime construction and `relatedBy_*` instantiation ·
**B** weight calibration and generator realism · **C** additional limitations ·
**D** cost model, window sensitivity and CDN-scale partitioning ·
**E** reproducibility.

> **The body is at the limit.** Any new result has to fit an existing table row
> or go to an appendix, or something else has to come out.

Section structure follows the pattern observed in NOMS papers: Introduction ·
Related Work · Session-Centric Knowledge Graph · Evaluation Methodology ·
Results · Discussion and Limitations · Conclusion.

## Build

```bash
pdflatex article && bibtex article && pdflatex article && pdflatex article
```

Three files are vendored in this directory so that local and Overleaf builds are
identical:

- `IEEEtran.cls` and `IEEEtran.bst`, because the local TeX Live is the *basic*
  scheme and does not ship them.
- `stfloats.sty` (from `sttools`, LPPL), which allows double-column floats at the
  bottom of a page. Without it Table I and the two full-width figures can only
  sit at page tops, and they stack.

The preamble also falls back on `lmtt` when URW Courier (`pcr`) metrics are
absent, which avoids Type 3 bitmap fonts in the PDF. On Overleaf and full
installations neither fallback fires.

## Float placement

Double-column floats have only two slots per page, top and bottom. Current
placement, each separated from the next by a full page of text:

| Float | First cited | Placed |
|---|---|---|
| Table I (related work) | p2 | bottom of p2 |
| Fig. 1 (ontology) | p2 | bottom of p3 |
| Fig. 2 (pipeline) | p3 | bottom of p4 |

`dbltopnumber` is capped at 1 so two full-width floats can never share a page
top. No float precedes its first citation.

## Figures

Schematics (Figs. 1 and 2) are draw.io drawings; sources are in
`figures/src-drawio/`. Data plots (Figs. 3–5) are generated by
`figures/make_figures_en.py` from the experiment outputs. See
[`figures/README.md`](figures/README.md).

## Before submitting

1. **Re-verify the NOMS 2027 CFP** (page limit, dates, template). The site does
   not exist yet; NOMS 2027 is expected in May 2027, venue to be announced. The
   rules encoded here come from the NOMS 2026 CFP.
2. **Fill in the public repository URL** in Appendix E before camera-ready.
3. **Confirm the acknowledgment.** The paper currently thanks Azion Technologies
   for authorizing the aggregate fingerprint measurement of Section IV. Any
   funding or grant acknowledgment still needs to be added.

## Portuguese version

[`../http-session-noms-pt/`](../http-session-noms-pt/) holds a Portuguese
rendering of the same paper, kept faithful in meaning, structure and formatting.
It is not bound by the NOMS page rules.
